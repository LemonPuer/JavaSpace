<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.mybatisplus.mapper.UserMapper">
    <!--Page<Buser> myPage(@Param("page") Page<Buser> page, @Param("age") Integer age);-->
    <select id="myPage" resultType="buser">
        select * from buser where password = #{password}
    </select>
</mapper>